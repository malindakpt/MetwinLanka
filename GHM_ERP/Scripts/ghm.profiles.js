"use strict";


function initializeProdProfileSubmit(formElem,rmselectid)
{
    //$(formElem).data("validator").settings.submitHandler = function (form)
    //{
    //    var select = $("#" + rmselectid);
    //    var vals = getSelectedValuesOfSelect(select);
    //    var nameTemplate = $(select).attr("name") + "[INDEX].Id";
    //    attachHiddenElementsForArray(formElem, vals, nameTemplate, "INDEX");

    //    /* CODE and COLOR for validation */
    //    var nameTemplate = $(select).attr("name") + "[INDEX].Code";
    //    attachHiddenElementsForArray(formElem, vals, nameTemplate, "INDEX");
    //    var nameTemplate = $(select).attr("name") + "[INDEX].Color";
    //    attachHiddenElementsForArray(formElem, vals, nameTemplate, "INDEX");
    //    var nameTemplate = $(select).attr("name") + "[INDEX].Description";
    //    attachHiddenElementsForArray(formElem, vals, nameTemplate, "INDEX");

    //    select.removeAttr("name");

    //    form.submit();
    //};
}